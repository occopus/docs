infra_name: dockerswarm_infra
user_id: somebody@somewhere.com

nodes:
  - &H
    name: head
    type: dockerswarm_head_node
  - &W
    name: worker
    type: dockerswarm_worker_node
    scaling:
      min: 2
    variables:
      head_node: head

dependencies:
  - [*W, *H]
