

STEP 1: START GATHER SERVICE (before building the workflow)
flowbster-gather.sh -s

STEP 2: SUBMIT:
flowbster-feeder.sh -h <ip of GENERATOR node> -i input-description-for-vina.yaml -d input-ligands.zip -d input-receptor.pdbqt -d vina-config.txt 

STEP 3: KILL GATHER SERVICE (after destroying the workflow):
flowbster-gather.sh -d

